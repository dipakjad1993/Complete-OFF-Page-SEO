services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-sqlite:///./offpage_seo.db}
      FRONTEND_ORIGINS: ${FRONTEND_ORIGINS:-http://localhost:3000}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      REQUIRE_AUTH: ${REQUIRE_AUTH:-false}
    volumes:
      - ./data:/app/data
      - search-cache:/app/search_cache.db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  frontend:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 3000"
    depends_on:
      backend:
        condition: service_healthy
  # Postgres option: uncomment for multi-brand parallel + scheduled re-runs at scale.
  # Then set DATABASE_URL=postgresql+psycopg2://offpage:offpage@db:5432/offpage in .env
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: offpage
  #     POSTGRES_USER: offpage
  #     POSTGRES_PASSWORD: offpage
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U offpage"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
volumes:
  search-cache:
  # pgdata:
