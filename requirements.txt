# requirements.txt — canonical (base + optional ML commented guidance).
# For lean installs:  pip install -r requirements-base.txt
# For full ML:        pip install -r requirements-ml.txt
-r requirements-base.txt
# Uncomment for local embeddings/NLP (heavy):
# -r requirements-ml.txt
